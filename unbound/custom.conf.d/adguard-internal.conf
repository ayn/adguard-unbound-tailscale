server:
    # Keep the process user unchanged so the container can update the bundled
    # RFC5011 trust anchor in /var/unbound.
    username: ""

    # The base image already listens on 0.0.0.0:53 and allows common RFC1918
    # bridge ranges. Add ULA IPv6 for future internal Docker use.
    access-control: fd00::/8 allow
